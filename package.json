{
  "name": "abn-search-express",
  "version": "1.0.0",
  "description": "abn-search with express",
  "main": "index.js",
  "scripts": {
    "build": "cd client/ && npm i && npm run build",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "christian crisologo",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.0"
  },
  "proxy": "http://localhost:8080"
}
